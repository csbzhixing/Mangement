<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mangement.mybatis.mappers.ContractMapper">
	<resultMap type="Contract" id="ContractResult">
		<id property="contract_ID" column="contract_ID" />
		<result property="name" column="name" />
		<result property="start_time" column="start_time" />
		<result property="finish_time" column="finish_time" />
		<result property="total_payment" column="total_payment" />
		<result property="unpayment" column="unpayment" />
		<result property="contract_type" column="contract_type" />
	</resultMap>


	<insert id="insert" parameterType="Contract">
		insert into contract
		values(#{contract_ID},#{name},#{start_time},#{finish_time},#{total_payment},#{unpayment},#{contract_type})
	</insert>

	<delete id="deleteById" parameterType="String">
		delete from
		contract where
		contract_ID=#{contract_ID}
	</delete>


	<select id="findById" parameterType="String" resultType="Contract">
		select
		* from contract where contract_ID=#{contract_ID}
	</select>

	<select id="find" resultMap="ContractResult">
		select * from contract
	</select>

	<update id="updateById" parameterType="Contract">
		update contract set
		name=#{name},start_time=#{start_time},finish_time=#{finish_time},total_payment=#{total_payment},unpayment=#{unpayment},contract_type=#{contract_type}
		where contract_ID=#{contract_ID}
	</update>

</mapper> 